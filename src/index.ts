function hello(nama: string, umur: number): string {
    return `Selamat Datang `+ nama +` di Kelas PWL, umur `+ nama +` adalah `+ umur +``;
}

const mahasiswa ='Andi';
const umur = 19;
console.log(hello(mahasiswa, umur));